import React from 'react';

type Props = {};

const Header = (props: Props) => {
  return (
    <>
      <header>
        <h1>Header</h1>
        {/* Logo: ShopSleek - Link.href -> / */}

        {/* Form (server action) */}
        {/* Icon: Magnifiying: solid */}
        {/* Input: name=searchTerm */}
        {/* Button: SearchBtn */}
        {/* Form */}
      </header>
    </>
  );
};

export default Header;
